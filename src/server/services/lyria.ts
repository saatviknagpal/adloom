/** Optional background music. */

export async function generateMusic(_mood: string, _durationSec: number): Promise<{ uri: string } | null> {
  // TODO: integrate Lyria API
  void _mood;
  void _durationSec;
  return null;
}
